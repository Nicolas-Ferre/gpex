# Test all forms of imports

dimensions:
  - id: import
    cases:
      absolute_root:
        form: "imported_root"
      absolute_nested:
        form: "nested.imported_inner"
      relative_direct_parent:
        form: "~.nested.imported_inner"
      relative_indirect_parent:
        form: "~.~.imports.nested.imported_inner"
