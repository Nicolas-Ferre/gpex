fn fn_(
    type_: const typeref,
    value: const ? require type_ != i32 || typeof(value) == type_ && value == 1,
) -> bool {
    return typeof(value) != i32;
}

var _result_valid = fn_(u32, 1u); // expected: true
