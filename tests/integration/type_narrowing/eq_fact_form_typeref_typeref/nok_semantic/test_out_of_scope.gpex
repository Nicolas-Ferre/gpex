fn _fn(
    left_type: const typeref,
    left: left_type,
    right_type: const typeref,
    right: right_type,
) -> bool {
    return left_type == i32
        && (left_type == right_type && left + right == 0 || true)
        && left + right == 0;
}
