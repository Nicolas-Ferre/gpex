fn add(
    left: const ? require typeof(left) == i32,
    right: const ? require left == 1 && typeof(right) == i32,
) -> i32 {
    return left + right;
}

var _result_three = add(1, 2); // expected: 3
