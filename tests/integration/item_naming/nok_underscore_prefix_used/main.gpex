// Test used items named with underscore prefix (must generate warnings).

// =========================| Global constants |=========================

const _CONST = 0;
var _const_usage = _CONST;

// =========================| Global variables |=========================

var _var = 0;
var _var_usage = _var;

// =========================| Functions |=========================

fn _fn() -> i32 { return 0; }
var _fn_usage = _fn();

// =========================| Function parameters |=========================

fn _fn(_param: i32) { _param = 0; }
