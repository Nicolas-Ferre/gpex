// Test items named with at least two characters (must not generate warnings).

// =========================| Global constant |=========================

pub const CO = 0;

// =========================| Global variable |=========================

pub var va = 0;

// =========================| Function |=========================

pub fn fu() -> i32 { return 0; }

// =========================| Function parameter |=========================

fn _fn(pa: i32) { pa = 0; }
